package wanghg.rhigosis;

terminal ID;
terminal CCE;
terminal BAR;
terminal LBRACK;
terminal RBRACK;
terminal LBRACE;
terminal RBRACE;
terminal LPAREN;
terminal RPAREN;
terminal LITERAL;
terminal NL;

nonterminal grammar;
nonterminal prod;
nonterminal rhs;
nonterminal term;
nonterminal options;

start with grammar;

grammar     ::= prod:p {: System.out.println(p); :};
grammar     ::= prod:p BAR NL {: System.out.println(p); :};
grammar     ::= grammar prod:p {: System.out.println(p); :};
grammar     ::= grammar prod:p BAR NL{: System.out.println(p); :};

prod        ::= ID:id CCE rhs NL {: RESULT = id; :};
prod        ::= prod:p BAR rhs NL {: RESULT = p; :};

rhs         ::= term;
rhs         ::= rhs term;

term        ::= LBRACK options RBRACK;
term        ::= LBRACE options RBRACE;
term        ::= LPAREN options RPAREN;
term        ::= LITERAL;
term        ::= ID;

options     ::= rhs;
options     ::= options BAR rhs;












